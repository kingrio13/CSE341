<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main>
            <% if (products.length > 0) { %>
                <ul class="cart-item">
                    <% products.forEach(p => { %>
                        <li>
                            <img src="<%= p.productData.imageUrl %>" alt="<%= p.productData.title %>" width="100px">
                            <p> <%= p.productData.title %> (<%= p.qty %>)</p>
                            <form action="/proveactivities/prove03/cart-delete-item" method="POST">
                                <input type="hidden" value="<%= p.productData.id %>" name="productId">
                                <button class="btn" type="submit">Delete</button>
                            </form>
                        </li>
                    <% }) %>
                </ul>
            <% } else { %>
                <h1>No Products in Cart!</h1>
            <% } %>
        </main>
        <%- include('../includes/end.ejs') %>